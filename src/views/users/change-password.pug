extends ../base.pug

block content 
  form(method="POST")
    if errorMessage 
      span=errorMessage
    input(placeholder="Old Password", type="password", name="oldPassword")
    input(placeholder="New Password", type="password", name="newPassword")
    input(placeholder="New Password Confirm", type="password", name="newPasswordConfirmation")
    input(type="submit", value="비밀번호 변경")